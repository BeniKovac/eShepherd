@model IEnumerable<web.Models.Jagenjcek>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdJagenjcka)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.kotitev)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.spol)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdJagenjcka)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.kotitev.KotitevID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.spol)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.skritIdJagenjcka">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.skritIdJagenjcka">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.skritIdJagenjcka">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
